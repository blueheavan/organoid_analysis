# Local reproducibility record

## Scoped workflow update — 2026-09-11

Started from clean `657204d8d8be2e547063272fd139161f59dc62be`; changes remain
uncommitted. [Current snapshot](evidence/2026-09-11/snapshot.json) records the
tracked patch hash, source/lock/package identities and relevant untracked files.
[The scoped report](WORKFLOW_OPTIMIZATION_2026-09-11.md) identifies the executed
336-pass default suite, remaining typecheck errors and bounded runtime probes.
The optional local real TIFF and saved-mask fixture have their own hashes;
neither qualifies biological accuracy. No new model inference, renderer suite
or distribution installation is claimed for this update.

Reproduce probes/source capture with
`pixi run python docs/evidence/2026-09-11/capture_evidence.py`.
Use `--snapshot-only` to refresh source identity without rerunning the probes.

## Historical full audit — 2026-09-10

The tested source starts at commit `ab6ee90db936de2a0888b9600ddf62f1729b4a1e` with a **dirty** worktree. [Initial status](evidence/2026-09-10/initial-status.txt) identifies pre-existing uncommitted edits; the audit preserved and revalidated them. No commit, push or release publication was performed.

[Final snapshot](evidence/2026-09-10/final-snapshot.json) records the exact final tracked-patch SHA-256, package/test/config file hashes, relevant untracked hashes, wheel SHA-256 and environment. The snapshot and its own generated patch/hash files are excluded from self-hashing. The tracked patch is retained as `full-tracked.patch`; runtime outputs have their own source hashes and may precede final type-only/reader-recovery edits. Final full regression and final artifact E2E cover the final implementation. Native render/model smoke cover the exercised paths, with host execution explicitly identified.

Environment: macOS arm64, locked Pixi workspace, Python 3.12.14; installed dependency inventory is [dependencies.json](evidence/2026-09-10/dependencies.json). The root `pixi.lock` is hashed. Installed versions include NumPy 2.5.2, SciPy 1.17.1, scikit-image 0.26.0, tifffile 2026.3.3, statsmodels 0.14.6, Cellpose 4.2.1.1, Torch 2.7.1, scikit-learn 1.9.0. This is an existing environment, not proof of a clean bootstrap on every platform. `uv pip check` reports duplicate VTK 9.6.2 dist-info/egg-info metadata; no environment files were deleted to conceal that finding.

## Reproduction commands

Run from repository root using its Pixi environment. Output directories must be new/empty.

```sh
pixi run test
pixi run test-render
pixi run lint
pixi run typecheck
pixi run check
pixi run ruff format --check src tests scripts
pixi run smoke
pixi run python docs/evidence/2026-09-10/audit_probe.py
pixi run python -m organoid_analysis demo --out /tmp/organoid-new-demo
```

`test` selects non-render tests by repository default. `test-render` separately executes native offscreen VTK; it needed host graphics access after a sandbox segfault. Eight browser tests require Playwright and were skipped, not validated by the native render pass. `check` finds no notebooks (NOT APPLICABLE).

The wheel was built with the declared setuptools backend in an isolated temporary copy and installed with `uv pip install --no-deps --no-index --target ...`; [artifact verification](evidence/2026-09-10/artifact-final.log) compares installed Python/static resources to source and runs executable entrypoints from outside the checkout. This reuses the locked dependencies; it is not a dependency-isolated installation. The initial `pip wheel` command failed because pip is absent; the declared backend provided the successful build. A first artifact viewer probe used an unsupported int64 fixture and correctly rejected it; the corrected supported-uint16 probe passed.

## Data and scientific resources

[Real-data inventory](evidence/2026-09-10/real-data-inventory.json) hashes five local TIFFs, their dtype/shape and current measured metadata. They are gitignored, have no documented independent annotation/assay qualification and are not redistributed. The acquisition metadata are reported, not externally calibrated. Complete XY/Z metadata were available for the colon and spheroid files only; their explicit manifest values were copied from current metadata, not guessed. File identity labels in the runtime manifest are **not** biological-replicate evidence.

The demo uses generator seed 1729; production bootstrap seed is 20260831. Synthetic truth is generated by this project, so exact identity/matching does not establish independent segmentation or viability validation. Small tables/config/provenance/results are retained under `evidence/2026-09-10/synthetic`, `real`, and `final-artifact`; large arrays remain in the named temporary output directories and can be regenerated for synthetic data. Temporary directories are ephemeral.

Cached `cpdino-vitb` weight identity is captured in the final snapshot; inference used MPS on one central 3×256×256 PDAC C1/C2 crop with default SegmentationConfig and batch size 1. Physical defaults in that smoke script are not validated calibration. It reports 29 nucleus/23 cell labels; no accuracy reference exists. Weight acquisition/license audit, alternate cache identity, full-volume accuracy, repeat-run mask equivalence and CPU/MPS/GPU equivalence remain NOT ASSESSED.

[Parameter registry](evidence/2026-09-10/parameters.json) records 113 consequential values/rules, including implicit Cellpose defaults. [Estimator defaults](evidence/2026-09-10/estimator-defaults.json) captures installed sklearn/XGBoost constructor parameters; XGBoost's recorded class-weight example is 1, while actual training derives its ratio from training labels. No cutoff was recalibrated on evaluation outputs.

## Provenance limitations and revalidation triggers

Classical/CLI runs retain source hashes, input hashes, configuration and environment. Web multilevel exports retain source and mask hashes but not a complete independently qualified assay/study/model chain. Model object identity can differ from a best-effort cache filename lookup. No complete SBOM, vulnerability scan, secret scan, annotation license qualification or external CI was performed; status NOT ASSESSED. Dependency inventory is not an SBOM.

Revalidate after changing input acquisition/calibration, model weights, dependency/OS/backend versions, segmentation/QC thresholds, statistical unit/design, or output semantics. Historical results affected by unit, identity, surface-coordinate or numerical fixes must be rerun and compared using their original inputs/configuration; no automatic certification of prior outputs is possible. A future positive release requires resolving the failed geometric criterion and obtaining qualified real-data and inferential evidence; review/approval alone cannot replace those data.
